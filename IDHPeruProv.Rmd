```{r}
library(rio)
RawIDH <- import_list('DATA/pe.Indice de Desarrollo Humano Perú.xlsx',rbind=FALSE)
RawIDH=RawIDH[[1]]
```

```{r}
HDI=RawIDH[,c(1:3,8)]
names(HDI)=c('Ubigeo', 'Dep', 'Dis', 'IDH')
HDI=HDI[c(1:2257),]
HDI=HDI[c(8:2257),]
row.names(HDI)=NULL
```

```{r}
HDI=HDI[,-c(2)]
HDI[,c(3)]=lapply(HDI[,c(3)],as.numeric)
HDI=na.omit(HDI)
row.names(HDI)=NULL
```

```{r}
library(stringr)

list=as.list(HDI[,1])
provinces=grep(pattern='[0-9]{1}[0-9]{1}[0-9]{1}[0-9]{1}[0]{1}[0]{1}',x=list)
HDI=HDI[c(provinces),]
names(HDI)=c('Ubigeo','Provincia','IDH')
```

